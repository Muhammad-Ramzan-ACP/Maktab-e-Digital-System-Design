#include<stdio.h>

/*
int max(int num1,int num2);
int main()
{
    int x = 32;
    int y = 5;
    int ret;

    ret = max(x,y);
    printf("Comparing two numbers using max() function: \n");
    printf("Which of the two, 32 or 5, is greater than the other? \n");
    printf("The answer is: %d", ret);

}
int max(int num1,int num2){
    if(num1>num2)
    {
        return num1;
    }
    else
    {
        return num2;
    }
    return 0;
}

*/